# demo-backup/ — ảnh dự phòng khi demo live hỏng

Chụp từ **bản deploy thật** `minihackathon.ai42e.com` ngày 31/07/2026, không phải máy local.

| Ảnh | Nội dung | Dùng thay cho nhịp nào |
|---|---|---|
| `01-boi-den-tra-loi-co-trich-dan.png` | Bôi đen đoạn trên slide → AI trả lời kèm badge phạm vi + citation số trang | Nhịp 1 (case chuẩn) |
| `02-ngoai-pham-vi-tu-choi.png` | Gõ `MCP là gì` → badge `⚠️ NGỮ CẢNH: NGOÀI PHẠM VI HỌC LIỆU` + `[Không tìm thấy trích dẫn hợp lệ]`, đo được `917ms` | **Nhịp 2 (case chỗ khó)** |
| `03-ke-hoach-va-gui-telegram.png` | AI nhận ra yêu cầu lập kế hoạch → thẻ 3 buổi có số trang thật → nút gửi Telegram | Nhịp 3 |

Đủ cả 3 nhịp của kịch bản demo. Ảnh nhịp 2 là quan trọng nhất — guide §5.1 ghi rõ *"case lỗi được xử lý là phần được đánh giá cao"*, và đây là ảnh duy nhất cho thấy sản phẩm **biết dừng lại khi không có căn cứ**.

**Cách dùng khi mạng hỏng:** nói thẳng *"Đang lỗi mạng hội trường, em cho xem kết quả đã đo trước"* rồi mở ảnh. Giám khảo trừ điểm vì lúng túng, không trừ vì mạng.
