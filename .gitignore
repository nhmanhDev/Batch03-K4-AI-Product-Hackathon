.DS_Store
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
venv/

# Log server chạy tay lúc dev (npm run start > server_ps*.log)
server_ps*.log
*.log

# Biến môi trường — KHÔNG commit API key (02-guide.md §3.4 luật an toàn ①)
# .env*  bắt .env · .env.local · .env.development.local · .env.production.local
# *.env  bắt prod.env · local.env
.env*
*.env
!.env.example

# Script cào dữ liệu & file thử nghiệm tạm thời
scripts/
scratch/

# Toàn bộ docs/ — research là dữ liệu cào từ VLearn thật, design-references là
# ảnh chụp màn hình VLearn thật (có thể dính watermark email cá nhân).
# Cả hai đều là tư liệu nội bộ, không phải sản phẩm nộp.
docs/

# TOÀN BỘ data pack của BTC — 02-guide.md §5.2: "không đổ nguyên data pack lên".
# Luật cũ chỉ chặn D[0-9]*.pdf nên 2 file viết thường d1/d2-slide-hackathon.pdf,
# chatlog và transcript vẫn lọt lên GitHub public. Chặn cả thư mục cho chắc.
# Lưu ý: các file này VẪN còn trong lịch sử git của những commit trước —
# gỡ hẳn cần viết lại lịch sử (git filter-repo + force push), làm sau deadline.
data/
day01-slide-blue-v0.pdf
